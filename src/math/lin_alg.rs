use crate::math::vec::Vec3;

pub fn draw_sphere(radius : f32, position : Vec3, sectorcount : i32, stackcount : i32 ) -> Vec<f32>
{
    let (x, y, z , xy) : (f32, f32, f32, f32);
    let (sectorangle, stackangle) : (f32, f32);
    let (s, t) : (f32, f32);
} 